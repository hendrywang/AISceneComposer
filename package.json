{
  "name": "ai-scene-composer",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "web": "pnpm --filter @asc/client web",
    "api": "pnpm --filter @asc/api dev"
  },
  "devDependencies": {
    "turbo": "^2.9.18",
    "typescript": "~6.0.3"
  }
}
